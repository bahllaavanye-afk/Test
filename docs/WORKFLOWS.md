# QuantEdge — Workflow / Employee Manifest

> Auto-generated by `scripts/gen_workflow_manifest.py`. Read-only inventory of
> every GitHub Actions workflow (the autonomous 'employees') so cadence and overlap
> are legible. Regenerate after adding/removing a workflow.

**Total workflows:** 87  |  **Scheduled (cron):** 70

## Consolidation candidates (families with ≥3 workflows)

- **`agent-*`** (6): agent-chatbot.yml, agent-health-check.yml, agent-health-monitor.yml, agent-heartbeat.yml, agent-self-review.yml, agent-status-check.yml
- **`daily-*`** (3): daily-employee-review.yml, daily-pnl-report.yml, daily-standup.yml
- **`gemini-*`** (3): gemini-change-review.yml, gemini-ml-training.yml, gemini-task-runner.yml
- **`render-*`** (5): render-autofix.yml, render-failure-issue.yml, render-fix-build.yml, render-fix-db-url.yml, render-monitor.yml
- **`slack-*`** (10): slack-agent-team.yml, slack-auto-review.yml, slack-bootstrap.yml, slack-digest.yml, slack-kickoff.yml, slack-on-deploy.yml, slack-pulse.yml, slack-summon-watcher.yml, slack-team.yml, slack-test.yml
- **`strategy-*`** (5): strategy-auto-tune.yml, strategy-autopilot.yml, strategy-health.yml, strategy-promotion.yml, strategy-trim.yml

## All workflows

| Workflow file | Name | Cron | Triggers | Purpose |
|---|---|---|---|---|
| `agent-chatbot.yml` | Agent Chatbot — Reply via Slack (works without C | — | workflow_dispatch | Agent Chatbot — Reply via Slack (works without Claude) |
| `agent-health-check.yml` | Agent Health Check | `*/15 * * * *` | workflow_dispatch | Agent Health Check |
| `agent-health-monitor.yml` | Agent Health Monitor — self-healing diagnostics | `*/30 * * * *` | workflow_dispatch, push, issues | Runs every 30 minutes + on every push that touches the agent script. |
| `agent-heartbeat.yml` | Agent Heartbeat — Zero-Downtime Monitor | `*/30 * * * *` | workflow_dispatch | Runs every 30 minutes to verify all critical agents are alive. |
| `agent-self-review.yml` | Agent Self-Review | — | push | Runs on every push to the agent branch when core agent files change. |
| `agent-status-check.yml` | Agent Status Check — Every 4 Hours | `0 */4 * * *` | workflow_dispatch | Agent Status Check — Every 4 Hours |
| `ai-pr-review.yml` | AI PR Review — Cursor-style review on every PR | — | pull_request | Posts an automated AI code review on every pull request (open/update). Advisory only — |
| `ai-slack-bot.yml` | QuantEdge AI Slack Bot | `0 9,13,17,21 * * *` | workflow_dispatch, push | Posts proactive AI insights 4x/day to all desks. |
| `auto-launch.yml` | Auto Launch — bootstrap Slack, add admin, all de | — | workflow_dispatch, push | Runs automatically on push to the dev branch. |
| `auto-merge.yml` | Auto-merge — land green PRs without a human | — | workflow_dispatch, workflow_run, pull_request_target, check_suite | Durable replacement for hand-merging the autonomous bots' PRs. Strictly opt-in: |
| `autonomous-strategy-generator.yml` | Autonomous Strategy Generator | `0 */3 * * *` | workflow_dispatch | Run daily — proposes and commits new strategies every day at 05:00 UTC |
| `backend-team.yml` | Backend AI Engineering Team | `0 */4 * * *` | workflow_dispatch, push | Reviews backend code for bugs, security, and performance issues. |
| `brain-health.yml` | Brain Health — LLM cascade canary | `17 * * * *` | workflow_dispatch | The free-LLM cascade is the agents' brain. It died silently once (Cloudflare |
| `channel-monitor.yml` | Channel Monitor — self-healing channel diagnosti | `*/30 * * * *` | workflow_dispatch | Reads every monitored channel, reports gaps, auto-heals silent agents. |
| `claude-chat.yml` | Claude ↔ Employee Conversations | `0 */2 * * *` | workflow_dispatch | Claude talks to all employees every 2 hours — no downtime |
| `collective-learning.yml` | Collective Learning Aggregator — Every 30 Minute | `*/30 * * * *` | workflow_dispatch | Collective Learning Aggregator — Every 30 Minutes |
| `company-brain.yml` | Company Brain Sync (Every 15 Minutes) | `*/15 * * * *` | workflow_dispatch | Company Brain Sync (Every 15 Minutes) |
| `context-sync.yml` | Context Sync | `*/30 * * * *` | workflow_dispatch | Context Sync |
| `continuous-improvement.yml` | Continuous Improvement — Autonomous Code Quality | `*/30 * * * *` | workflow_dispatch | Every 30 minutes — 48 runs/day × 3 commits each = 144+ commits/day toward CTO OKR |
| `daily-employee-review.yml` | Daily Employee Review — strict daily scoring | `0 7 * * *` | workflow_dispatch | Runs every day at 07:00 UTC. Reviews every employee's Slack presence, |
| `daily-pnl-report.yml` | Daily P&L Attribution Report | `30 */4 * * *` | workflow_dispatch | 21:30 UTC weekdays = ~5:30 PM ET (30 min after market close reconciliation) |
| `daily-standup.yml` | Daily Standup — All Employees (13:00 UTC) | `0 13 * * *`<br>`30 13 * * *`<br>`0 17 * * *`<br>`30 20 * * *` | workflow_dispatch | Daily all-hands standup at 13:00 UTC per org cadence |
| `deep-code-review.yml` | Deep Code Review Agent Pool (Daily) | `0 6 * * *` | workflow_dispatch | Deep Code Review Agent Pool (Daily) |
| `deploy-verify.yml` | Deploy Verify — live app is the real API (drift  | `0 */6 * * *` | workflow_dispatch, push | Catches the failure that hid for a month: a stub / wrong app deployed instead of |
| `desk-trading-crypto-24x7.yml` | Desk Trading — Crypto 24/7 (Paper Orders) | `7,27,47 * * * *` | workflow_dispatch, push | Crypto markets trade 24/7, but desk-trading.yml gates ALL desks to the US |
| `desk-trading.yml` | Desk Trading (Paper Orders) | `*/15 9-22 * * 1-5` | workflow_dispatch, push | Every 15 minutes, Mon–Fri, 09:30–22:00 UTC (covers US market hours + pre/post) |
| `e2e-demo.yml` | E2E Demo with Live Data | — | workflow_dispatch | Manual-only trigger. Auto-trigger on push was failing every push because |
| `employee-conversations.yml` | Employee Conversations (hourly) | `5 * * * *` | workflow_dispatch | Extract provider info (lines containing "via " pattern) |
| `employee-intros.yml` | Daily Employee Introductions (2-Round Conversati | `0 9 * * *` | workflow_dispatch | Daily Employee Introductions (2-Round Conversations) |
| `fill-tracking.yml` | Fill Tracker — Strategy P&L Attribution | `0 22 * * 1-5` | workflow_dispatch | Run at 22:00 UTC Mon–Fri (after US market close + settlement buffer) |
| `free-agent-engineer.yml` | Free Agent Engineer — autonomous issue fixing | `0 */2 * * *` | workflow_dispatch, issues | Fires when "agent-fix-needed" label is added to any issue, or every 4 hours, |
| `frontend-design-agent.yml` | Frontend Design Agent — Continuous UI/UX Improve | `0 */2 * * *` | workflow_dispatch | Every 6 hours — Priya Iyer (VP Frontend) is always improving the dashboard |
| `frontend-team.yml` | Frontend Team — AI-Driven UI Improvements | `0 3 * * 1,3,5` | workflow_dispatch | Automated frontend improvements: QuantEdge AI reviews the UI, proposes changes, |
| `gemini-change-review.yml` | Gemini Change Review — auto-review every AI comm | `0 */6 * * *` | workflow_dispatch, push | Reviews every batch of AI-generated commits. |
| `gemini-ml-training.yml` | Gemini Free Cloud ML Training | `0 */4 * * *` | workflow_dispatch | Run daily at 01:00 UTC (before nightly retrain at 02:00) |
| `gemini-task-runner.yml` | Gemini Task Runner — free-LLM code changes | `*/20 * * * *` | workflow_dispatch, push, issues | Picks up open issues labeled "gemini-task", sends each to Gemini Flash (or |
| `investor-pipeline-update.yml` | Investor Pipeline — CEO OKR Update | `0 */4 * * *` | workflow_dispatch | Investor Pipeline — CEO OKR Update |
| `keep-alive.yml` | Keep-Alive — stop the Render backend from sleepi | `*/10 * * * *` | workflow_dispatch | Render's free web service sleeps after ~15 min of no HTTP traffic. When it sleeps the |
| `live-trading-reporter.yml` | Live Trading Reporter (Paper Account) | `*/30 9-22 * * 1-5` | workflow_dispatch | Every 30 minutes on market days: reads real Alpaca paper account data |
| `lstm-training.yml` | LSTM Model Training (CPU — Weekly) | `0 2 * * 0` | workflow_dispatch | Weekly CPU training on real OHLCV data (SPY + BTC daily, 3 years). |
| `market-opinion.yml` | Daily Market Opinion — Morning Brief + Strategy  | `0 14 * * 1-5` | workflow_dispatch | 09:00 ET = 14:00 UTC on weekdays |
| `market-scanner.yml` | Market Scanner — Intraday Regime + Sector + Brea | `0 14 * * 1-5`<br>`0 15 * * 1-5`<br>`0 16 * * 1-5`<br>`0 17 * * 1-5`<br>`0 18 * * 1-5`<br>`0 19 * * 1-5`<br>`0 20 * * 1-5` | workflow_dispatch | Market open: 14:00 UTC (9:00 ET), 15:00, 16:00, 17:00, 18:00, 19:00, 20:00 UTC |
| `memory-consolidator.yml` | Memory Consolidator — Nightly Knowledge Distilla | `0 2 * * *` | workflow_dispatch | Memory Consolidator — Nightly Knowledge Distillation |
| `migrate.yml` | Migrate Supabase Schema | — | workflow_dispatch | Run on demand from the GitHub UI: |
| `multi-agent-discussion.yml` | Multi-Agent Discussion — Every 2 Hours | `0 */2 * * *` | workflow_dispatch | Multi-Agent Discussion — Every 2 Hours |
| `notion-sync.yml` | Notion ↔ GitHub Issues Sync | `0 */6 * * *` | workflow_dispatch, issues | Mirrors GitHub Issues into a Notion task database. |
| `okr-tracker.yml` | OKR Tracker — Daily C-Suite Report | `0 9 * * *`<br>`0 14 * * 1` | workflow_dispatch, issues | Monday 14:00 run = weekly report |
| `p0-watchdog.yml` | P0 Watchdog — SLA Monitor | `0 * * * *` | workflow_dispatch, issues | P0 Watchdog — SLA Monitor |
| `page-reporter.yml` | Page Reporter — Real Screenshots + Employee Audi | `30 */4 * * *` | workflow_dispatch | Every 4 hours: take live browser screenshots of all dashboard pages, |
| `peer-review.yml` | Peer Review — Multi-Agent Code Review | `0 */3 * * *` | workflow_dispatch | Runs every 3 hours — reviews last 5 agent commits |
| `quant-firm-pipeline.yml` | QuantEdge Quant Firm Pipeline (Hourly) | `0 * * * *` | workflow_dispatch | QuantEdge Quant Firm Pipeline (Hourly) |
| `quick-backtest.yml` | Quick Backtest Runner — Every 15 Minutes | `*/15 * * * *` | workflow_dispatch | Quick Backtest Runner — Every 15 Minutes |
| `render-autofix.yml` | Render — Auto-Fix (build + DATABASE_URL) on merg | — | workflow_dispatch, push | Runs automatically when merged to main (and re-runnable manually). |
| `render-failure-issue.yml` | Render Failure → GitHub Issue Bridge | `*/10 * * * *` | workflow_dispatch, issues, repository_dispatch | When a Render deploy fails, creates/updates a GitHub Issue with the full logs. |
| `render-fix-build.yml` | Render — Fix Build Command (remove alembic from  | — | workflow_dispatch, push | Forces the Render service's build command to `pip install -e "."` (no alembic |
| `render-fix-db-url.yml` | Render — Fix DATABASE_URL (Supabase pooler) | — | workflow_dispatch | One-click fix: rewrites the Render DATABASE_URL from the Supabase DIRECT host |
| `render-monitor.yml` | Render Health Monitor & Auto-Fix | `*/15 * * * *` | workflow_dispatch | Checks Render deployment every 15 minutes. |
| `research-to-trade.yml` | Research → Trade Pipeline (24/7) | `*/30 * * * *` | workflow_dispatch | Research → Trade Pipeline (24/7) |
| `reward-gate.yml` | Reward Gate — verifiable-reward auto-merge for a | `*/20 * * * *` | workflow_dispatch | Sweeps open PRs labelled `agent-auto-merge` and merges one ONLY when it earns a |
| `run-experiments-agent.yml` | Run Experiments Agent | `0 */2 * * *` | workflow_dispatch | Run Experiments Agent |
| `run-experiments.yml` | Run ML Experiments | `0 */6 * * *` | workflow_dispatch | Every 6 hours: 00:00, 06:00, 12:00, 18:00 UTC |
| `secrets-check.yml` | Secrets & Config Health Check | `0 */6 * * *` | workflow_dispatch | Verifies all required secrets are present and reports missing ones to Slack. |
| `secrets-health-check.yml` | Secrets Health Check — Run This First | — | workflow_dispatch | Secrets Health Check — Run This First |
| `signal-runner.yml` | Signal Runner — Every 5 Minutes (All Desks) | `*/20 * * * *` | workflow_dispatch | Signal Runner — Every 5 Minutes (All Desks) |
| `slack-agent-team.yml` | Slack Agent Team — real engineering work, real r | `15 * * * *` | workflow_dispatch, issues | Each run wakes 6-15 agents. Each agent reads actual repo state — git log, |
| `slack-auto-review.yml` | Slack Auto-Review Loop (Every 5 min) | `*/20 * * * *` | workflow_dispatch | Slack Auto-Review Loop (Every 5 min) |
| `slack-bootstrap.yml` | Slack Bootstrap — create all 31 channels | — | workflow_dispatch, issues | Run from the Actions tab → Slack Bootstrap → Run workflow. |
| `slack-digest.yml` | Slack Digest | `0 13 * * 1-5` | workflow_dispatch | Runs the Slack message monitor on a schedule (and on demand), triaging every |
| `slack-kickoff.yml` | Slack Kickoff — fill all channels with team chat | — | workflow_dispatch, issues | Run from the Actions tab → Slack Kickoff → Run workflow. |
| `slack-on-deploy.yml` | Slack — Post-Deploy Notification | — | push | Slack — Post-Deploy Notification |
| `slack-pulse.yml` | Slack Pulse — Keep Team Informed | `0 * * * *` | workflow_dispatch, push | Posts deploy notifications, daily P&L, and system status to Slack. |
| `slack-summon-watcher.yml` | Slack Summon Watcher — answer @agent within 5 mi | `*/20 * * * *` | workflow_dispatch | Scans every Slack channel every 5 minutes for @agent / @quant / ask: / ?? messages |
| `slack-team.yml` | Slack Team — bootstrap, invite admin, employee d | `15 * * * *` | workflow_dispatch, push, issues | Runs from the DEFAULT branch (main) so cron actually fires. |
| `slack-test.yml` | Slack Webhook Verify | — | workflow_dispatch | Run from the Actions tab → Slack Webhook Verify → Run workflow. |
| `strategy-auto-tune.yml` | Strategy Auto-Tuner — Confidence Threshold Adjus | `30 22 * * 1-5` | workflow_dispatch | Run at 22:30 UTC Mon–Fri (30 min after fill tracker completes) |
| `strategy-autopilot.yml` | Strategy Autopilot — Auto-Explore, Backtest, Pro | `0 */6 * * *` | workflow_dispatch | Every 6 hours — explore, backtest, promote top variants |
| `strategy-health.yml` | Strategy Health Ranking | `0 22 * * 1-5` | workflow_dispatch | 22:00 UTC weekdays — after market close + P&L report |
| `strategy-promotion.yml` | Strategy Promotion Pipeline | `0 9 * * *` | workflow_dispatch | Strategy Promotion Pipeline |
| `strategy-trim.yml` | Strategy Trimmer (continuous) | `41 */6 * * *` | workflow_dispatch, push | Option-Alpha-style "always trimming": retire persistent paper losers so the desk |
| `system-status.yml` | System Status (auto-diagnose) | `23 */3 * * *` | workflow_dispatch, push | Probes brain + Slack + Alpaca + backend and commits a fresh SYSTEM_STATUS.md so |
| `system-watchdog.yml` | System Watchdog — Every 5 Minutes (Self-Healing) | `*/20 * * * *` | workflow_dispatch | System Watchdog — Every 5 Minutes (Self-Healing) |
| `team-lead-issues.yml` | Team Lead Issue Generator — All Teams | `0 * * * *` | workflow_dispatch, issues | Every day at 07:00 UTC — team leads file issues before stand-up |
| `test.yml` | CI | — | workflow_dispatch, pull_request | CI-only dep list — fast install (<60s), no heavy ML libs (torch, ray, mlflow, |
| `third-party-monitor.yml` | Third-Party Health Monitor | `*/30 * * * *` | workflow_dispatch | Checks all external services QuantEdge depends on. |
| `token-usage-monitor.yml` | Token Usage Monitor — Every 15 Minutes | `*/15 * * * *` | workflow_dispatch | Token Usage Monitor — Every 15 Minutes |
| `tv-indicator-improvement.yml` | TV Indicator SOTA Improvement (Agent Pool) | `0 */6 * * *` | workflow_dispatch | TV Indicator SOTA Improvement (Agent Pool) |
| `vercel-deploy.yml` | Vercel Production Deploy (auto) | — | workflow_dispatch, push | Zero-manual-steps production deploys: on every push to main that touches the |

